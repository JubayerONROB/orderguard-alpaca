# Improvement Changelog

| STAGE | WHAT I TRIED AND WHY | EVIDENCE | DECISION / LEARNING |
|---|---|---|---|
| Rule engine, single-pass repair | Repair each failing rule in sequence, re-checking after each. | `rules_only` 14/18. case_017 failed on `rules_fired`. | Not kept. R4's cancellation incidentally satisfied R3, so R3 was never recorded as having fired. The user would never learn their concentration cap had been at risk. |
| Two-phase evaluation | Check every repairable rule against the ORIGINAL plan first to establish what fired, then resolve sequentially (R2 -> R6 -> R4 -> R3), then full re-check. | `rules_only` 18/18, catch_rate 100%, false_block_rate 0%. | Kept. A repair that silently fixes a second problem is a reporting bug. |
| Fixture isolation defect | n/a - discovered while implementing R1. | case_009/015/016 fired R3 as well as R1. | Those fixtures had equity too small for the intended single-rule isolation. Fixed the FIXTURES (added an untouched position to raise equity); expected decisions and expected rules_fired were NOT changed. Disclosed here deliberately. |
| Baseline vs agent: live eval | Ran both systems live against all 18 cases (agent: real IntentCompiler call -> RuleEngine; baseline: one Agnes call given the identical rendered account/market context and told all seven rules by name and definition), no prompt changes made after seeing either result. Reproduced both afterward via cassette replay to confirm determinism. | `rules_only` 18/18 (100% primary, 100% catch_rate, 0% false_block_rate). `agent` 16/18 (88.9% primary, 90.9% catch_rate, 0% false_block_rate, mean_latency 13.05s live / 0.004s replay, 18 llm_calls). `baseline` 12/18 (66.7% primary, 54.5% catch_rate, 0% false_block_rate, mean_latency 38.01s live / 0.001s replay, 18 llm_calls). Replay reproduced both exactly. | Kept as-is, not tuned. The deterministic engine alone is perfect; wiring it behind a real compiler costs ~11 points off primary_score -- both misses are genuine compiler drift, not engine bugs: case_003's compiler pre-emptively cancelled the stacking open order itself (not asked to; the final basket was still correct, but it changed what the engine had left to detect, so R4_OPEN_ORDERS never fired and rules_fired mismatched); case_006 rounded a "roughly 12%" rebalance to the NEAREST share instead of down, landing 1 share over on two orders while still safely under the 15% cap. The single-prompt baseline, doing both jobs unaided in one pass, loses another ~22 points of primary_score and ~36 points of catch_rate versus the agent -- concrete evidence for the architecture, not just a stated preference. |
| A1: R4 detection independent of remediation | Changed R4's detection to fire whenever the account has an existing open order on a symbol the basket touches, REGARDLESS of whether the plan already proposes cancelling it (previously detection excluded already-cancelled ids, so a plan that pre-emptively cancelled the conflict made it invisible). Added `always_repairable` to `RepairableRule` so the engine can tell "repair() no-op because already fixed" (R3/R4, disposition REPAIRED) apart from "repair() no-op because no fix exists" (R2/R6, disposition BLOCKED) -- previously both were treated as BLOCKED. Added a unit test: plan already contains the cancellation, R4 must still fire as REPAIRED. | `rules_only` unaffected (still 18/18 -- none of its static naive_plans pre-cancel anything). New tests `test_open_orders_still_fires_when_plan_already_cancels_it` and `test_engine_records_r4_when_naive_plan_already_cancels_it` both pass. Live agent re-run (combined with A2, see next entry) shows case_003 correctly reporting R4_OPEN_ORDERS as fired/REPAIRED. | Kept. Detection must not depend on who (or what) already fixed the problem -- otherwise an upstream system's good behavior (or luck) can silently suppress the audit trail the trader is supposed to see. |
| A2: compiler rounds shares down (compile_v2.md) | case_006's compiler rounded a "roughly 12%" rebalance to the nearest share instead of down (61/62 instead of 60/61), still safe but not matching the intended convention. Added an explicit rounding-DOWN instruction to a new `compile_v2.md` (kept `compile_v1.md` in the repo, unused, for this record) and pointed `IntentCompiler` at v2. Re-ran `agent` live (bumped by A1 + A2 together; baseline untouched, its cassettes stayed valid). | Before (v1 prompt, live): agent 16/18, 88.9% primary, 90.9% catch_rate. After (v2 prompt + A1, live): agent 18/18, 100.0% primary, 100.0% catch_rate, 0% false_block_rate, mean_latency 20.41s live / reproduced exactly via replay. | Kept. Both A1 and A2 moved the score; agent now matches `rules_only` exactly, meaning every remaining gap between `agent` and `baseline` is attributable to the architecture, not to compiler quirks this pass fixed. |
| Held-out suite (case_101-104) | The main 18 cases drove two fixes (A1, A2), so the agent's 100% on them is partly fitted. Wrote 4 new cases in `eval/cases_holdout/` without reading the rule implementations or compiler prompt -- from the case format and the seven rule definitions only -- covering shapes not in the main set: a relative target ("cut my tech exposure in half") spanning three positions with one opened today, extended-hours session interacting with buying power AND concentration across two repair stages, a sale funding two buys where only one breaches the cap, and a deliberately confusingly-phrased but safe instruction. Ran each system ONCE, live, no iteration. One correction made BEFORE any live run: `rules_only` (free, deterministic, no system change) caught that my own case_102 ground truth was wrong -- I'd checked AAPL's $3,000 against the $3,750 cap but forgotten MSFT's $5,000 also breaches it before R6 even removes it, so R3_CONCENTRATION also fires (and gets repaired as a side effect of R6's fix, same pattern as case_017). This is a fix to my own case authoring, verified via the deterministic engine, not a change to the system prompted by a live result. | `rules_only` (holdout) 4/4 -- 100% primary, 100% catch_rate, 0% false_block_rate. `agent` (holdout, live) 4/4 -- 100% primary, 100% catch_rate, 0% false_block_rate, mean_latency 28.80s, 4 llm_calls. `baseline` (holdout, live) 2/4 -- 50% primary, 66.7% catch_rate, 0% false_block_rate, mean_latency 46.19s, 4 llm_calls (missed case_102's rules_fired and case_103's orders). | NO CHANGES MADE to the system after these live results, as instructed. Reported as-is: `rules_only` and `agent` hit 4/4 on genuinely unseen cases (the two fixes generalized, not just fit the 18); `baseline` degraded further (50% vs 66.7% on the main suite), consistent with the main-suite finding that doing compile-and-check unaided in one pass does not hold up as cases get harder. This is the finding, not a bug to fix. |
